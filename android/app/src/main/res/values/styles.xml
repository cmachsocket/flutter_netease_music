<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 2026-08-24 适配:
         - 用户需求: 不要全屏应用, 状态栏/导航栏保留系统自己的位置 + 颜色,
           Flutter UI 在系统栏下方绘制 (传统 Android 风格)。
         - AndroidManifest 里 MainActivity 加了
           androidx.window.WindowOptOutEdgeToEdgeEnforcement=true meta-data,
           退出 Android 15+ 默认强制 edge-to-edge。
         - 主题改用 @android:style/Theme.Material.Light.NoActionBar
           (没 .NoTitleBar 全屏隐式默认, 是 Material 风格的 NoActionBar)。
         - 显式关掉 windowFullscreen, status bar / navigation bar 用半透明黑 (浅色主题)
           或 半透明白 (夜间主题) - 让状态栏文字/图标跟系统主题一致。
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <!-- 浅色主题: 状态栏背景半透明黑, 图标白色 -->
        <item name="android:statusBarColor">#80000000</item>
        <item name="android:navigationBarColor">#80000000</item>
    </style>
</resources>
