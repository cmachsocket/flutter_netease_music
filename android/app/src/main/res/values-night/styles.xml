<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 2026-08-24 适配: 同 values/styles.xml, 但 Dark 主题用 Theme.Material 父主题,
         状态栏背景半透明白 (夜间模式系统栏亮色背景, 图标深色)。
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:statusBarColor">#80FFFFFF</item>
        <item name="android:navigationBarColor">#80FFFFFF</item>
    </style>
</resources>
